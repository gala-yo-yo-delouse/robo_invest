# Lambda runtime deps — excludes streamlit/plotly (dashboard-only).
# boto3 is preinstalled in the Lambda base image. pandas is used by alpaca-py
# (was transitive before) so we pin it explicitly here.
alpaca-py>=0.30.0
pyyaml>=6.0
python-dotenv>=1.0.0
pandas>=2.0.0
